---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

# Installation

## Quick Install

To fully integrate Automock into your testing and dependency injection  frameworks, **you'll need to install two
packages: the Automock package for your chosen testing framework and the corresponding DI framework adapter.**

For **Jest**:

```bash npm2yarn
$ npm i -D @suites/jest
```

For **Sinon**:

```bash npm2yarn
$ npm i -D @suites/sinon
```

**And for your DI framework, choose the appropriate Automock adapter:**

<Tabs groupId="suites-adapter" className="adapters-tabs" queryString="suites-adapter">
  <TabItem attributes={{className: 'nestjs'}} value="nestjs" label="NestJS" default>
    <CodeBlock language="bash">
      {`$ npm i -D @suites/adapters.nestjs`}
    </CodeBlock>
  </TabItem>
  <TabItem attributes={{className: 'inversifyjs'}} value="inverisfyjs" label="InversifyJS">
    <CodeBlock language="bash">
      {`$ npm i -D @suites/adapters.inversify`}
    </CodeBlock>
  </TabItem>
</Tabs>

| **DI Framework Adapter**                               | **Jest (`@suites/jest`)** | **Sinon (`@suites/sinon`)** |
|--------------------------------------------------------|-----------------------------|-------------------------------|
| **NestJS Adapter (`@suites/adapters.nestjs`)**       | :white_check_mark:          | :white_check_mark:            |
| **Inversify Adapter (`@suites/adapters.inversify`)** | :white_check_mark: (Beta)   | :white_check_mark: (Beta)     |


## Seamless Integration with Different DI Frameworks

Automock extends its capabilities to integrate with various dependency injection  frameworks, such as NestJS and
InversifyJS, among others. Detailed insights into Automock's adaptability across frameworks are provided on our
dedicated [Adapters](/docs/adapters/intro) page.
